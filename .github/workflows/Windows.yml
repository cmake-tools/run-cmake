on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    name: test run-cmake
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: ⬇️ Setup CMake
        uses: lukka/get-cmake@latest
        with:
          cmakeVersion: ^3
          ninjaVersion: ^1.11.1
      - name: run-cmake
        uses: ./
        id: hello